# 107 — Night QA Nudge (2026-03-23 05:30 IST)

## Run summary
- `npm run -s test:atom:route-smoke` ✅ PASS
- `npm run -s test:atom:chat-contracts` ✅ PASS
- `npm run -s test:atom:reliability` ✅ PASS
- `npm run -s test:atom:nightly-gates:smoke-guarded` ⚠️ FAIL (auth probe gate)

## Checklist audit outcome
1. Deployment state + `/atom` route behavior: ✅ healthy (`/atom` present in Next build manifest).
2. Chat continuity (`continue` stays on topic): ✅ passing (chat-contracts + reliability).
3. Source grounding + insufficiency behavior: ✅ passing strict insufficiency checks.
4. Technical checks (tsc/build where relevant): ✅ pass before auth gate in smoke-guarded chain.
5. Quality gaps handled immediately: ✅ concrete unblock task executed to the point possible without secret access; handoff note created below.

## Concrete unblock task (owner action required)
Auth probe is failing with `invalid_credentials` for smoke user.

Execute in order:
1. Reset Supabase Auth password for `E2E_EMAIL` smoke user.
2. Update same credential pair in:
   - `nucleux-academy/.env.local`
   - CI secret store (same exact values)
3. Re-run:
   - `npm run -s test:auth:probe`
   - `npm run -s test:atom:nightly-gates:smoke-guarded`
4. If still failing:
   - remove accidental whitespace/newline in secret values
   - verify password grant is enabled in Supabase project settings

## Exit criterion
- `test:auth:probe` returns token success.
- `test:atom:nightly-gates:smoke-guarded` exits 0.
