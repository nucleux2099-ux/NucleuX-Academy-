# 105 — Night QA Nudge (2026-03-23 04:30 IST)

## Run summary
- `npm run -s test:atom:nightly-gates` ✅ PASS
- `npm run -s test:deploy:health` ✅ PASS (`/atom` returned 200)
- `npm run -s test:auth:probe` ⚠️ FAIL (`invalid_credentials`)
- `npm run -s test:atom:nightly-gates:smoke-guarded` ⚠️ FAIL at credentialed auth probe step

## Checklist audit outcome
1. Deployment state + `/atom` route behavior: ✅ healthy (200 on prod health check).
2. Chat continuity (`continue` stays on topic): ✅ passing route + reliability checks.
3. Source grounding + insufficiency behavior: ✅ strict insufficiency language check passing.
4. Technical checks (typecheck/build): ✅ green inside nightly gates.
5. Quality gaps handled immediately: ✅ concrete fix task staged below.

## Concrete fix task (execute at first maintainer availability)
- Reset Supabase smoke user password for `E2E_EMAIL`.
- Update the same `E2E_EMAIL`/`E2E_PASSWORD` pair in:
  - local `.env.local`
  - CI secret store
- Re-run:
  1) `npm run -s test:auth:probe`
  2) `npm run -s test:atom:nightly-gates:smoke-guarded`
- If still failing: verify no trailing whitespace/newline in secrets and password grant still enabled.

## Exit criterion
- `test:auth:probe` returns token success.
- `test:atom:nightly-gates:smoke-guarded` exits 0.
